# URL base da API do acervo (inclui o prefixo /api).
# Sem NEXT_PUBLIC_ prefix não seria legível no build client; aqui é público
# de propósito: é uma API somente-leitura, sem segredo algum.
NEXT_PUBLIC_API_URL=http://localhost:8000/api
